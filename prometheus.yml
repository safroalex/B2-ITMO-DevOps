global:
  scrape_interval: 10s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets:
          - localhost:9090

  - job_name: airflow
    metrics_path: /metrics
    static_configs:
      - targets:
          - airflow-webserver:8080
